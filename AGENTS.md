# Nya 仓库协作说明

本文件为代码代理提供稳定、简短的仓库地图。详细事实应保存在源码、测试或 `docs/` 中，不要把长篇架构说明复制到这里。

## 项目地图

- `packages/core/src/`：`@nya/core` 实现。
- `packages/core/tests/`：核心行为测试。
- `playground/`：示例与手动验证场景。
- `docs/concepts.md`：当前实现的概念说明。
- `docs/design.md`：提议中的目标设计；不能单独证明功能已实现。
- `docs/adr/`：已接受的架构决策。
- `docs/README.md`：完整文档地图和权威规则。

## 事实与冲突处理

1. 描述当前可观察行为时，以源码、导出类型和测试为直接证据。
2. 已接受的规范与 ADR 表达必须维持的设计约束；若实现偏离，不要静默修改任一方。
3. `docs/concepts.md` 应只描述有源码或测试支撑的当前行为。
4. `docs/design.md` 中未标记为已实现的内容属于目标设计。
5. 发现冲突时，在同一变更中修正文档，或明确报告冲突和需要决策之处。

## 工作命令

```bash
npm run build
npm test
npm run typecheck
npm run docs:check
npm run check
```

完成代码变更前，应运行与改动范围相称的验证；默认运行 `npm run check`。

## 实现约束

- 保持 TypeScript strict 模式，不用降低类型约束来绕过问题。
- 生命周期、依赖解析、清理顺序或作用域行为发生变化时，必须更新或增加测试。
- Effect 必须有明确所有者和可重复调用安全的清理路径。
- 不把 Context 隔离描述为权限或安全沙箱。
- 公共 API 或可观察语义变化时，同步更新 `docs/concepts.md`。
- 新的跨模块架构决策应在 `docs/adr/` 中记录。

## 文档约束

- 一篇文档只承担一种主要用途：教程、操作指南、解释、参考、规范或 ADR。
- 能从类型、Schema 或代码生成的参考内容不要手工复制维护。
- 示例应尽可能可以编译或由测试覆盖；无法自动验证时应明确说明。
- 不用 AI 生成结果作为事实来源。AI 可以起草和检查，最终陈述必须能追溯到代码、测试、已接受规范或外部一手资料。
- 修改 Markdown 后运行 `npm run docs:check`。
