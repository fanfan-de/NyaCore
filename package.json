{
  "name": "nya",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fanfan-de/NyaCore.git"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "type": "module",
  "scripts": {
    "build": "npm run build --workspace @nya/core && npm run build --workspace @nya/logger-console",
    "test": "npm run test --workspace @nya/core && npm run build --workspace @nya/core && npm run test --workspace @nya/logger-console",
    "typecheck": "npm run typecheck --workspace @nya/core && npm run build --workspace @nya/core && npm run typecheck --workspace @nya/logger-console && npm run build --workspace @nya/logger-console && npm run typecheck --workspace @nya/playground",
    "docs:check": "node scripts/check-docs.mjs",
    "check": "npm run docs:check && npm run typecheck && npm test",
    "package:check": "npm run build && node scripts/check-package.mjs",
    "release:check": "npm run check && node scripts/check-package.mjs",
    "dev:core": "npm run build --workspace @nya/core -- --watch",
    "dev:playground": "npm run dev --workspace @nya/playground",
    "playground": "npm run build && npm run start --workspace @nya/playground"
  },
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.5"
  }
}
